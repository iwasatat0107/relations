<div class="container text-center">
<div class="contents row">
  <% if user_signed_in? %>
    <div class="container">
      <h5><i class="far fa-address-card fa-2x"></i> 作成</h5>
      <%= render partial: "form", locals: { client: @client } %>
    </div>
  <% end %>
</div>